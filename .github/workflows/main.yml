name: Keep Legal Tech Alive

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping_services:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Legal Tech Vercel
        run: curl -s https://otaibi-law-q395.vercel.app/
      
      - name: Ping Supabase API
        # استخدام رابط الـ API الخاص بك
        run: curl -s https://nxycnbsaanjzsufziaun.supabase.co/rest/v1/
        
      - name: Log Success
        run: echo "Ping sent successfully to both Legal Tech and Supabase!"
